"""Reachset: effective reachability sets for identities across SaaS apps.

Owns nothing itself; see submodules. The dependency direction is strictly
connectors -> ingest -> (linking, reach) -> detections -> (api, mcp, triage).
"""

__version__ = "0.1.0"
